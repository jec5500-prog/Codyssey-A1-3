import SavedGallery from '@/components/saved/SavedGallery';

export const metadata = {
  title: 'Saved Spatial Collections | SPOT',
  description: 'Bookmarked spatial design field photos, material palettes, and collections.',
};

export default function SavedPage() {
  return <SavedGallery />;
}
